services:
  - type: web
    name: tms-training-center
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3002
      - key: NODE_ENV
        value: production
